(()=>{const data=window.LIDO_DIRECTORS||[];const grid=document.querySelector('[data-director-grid]');const input=document.querySelector('[data-director-filter]');const count=document.querySelector('[data-director-count]');if(!grid)return;const fmt=value=>new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(new Date(`${value}T12:00:00`));function render(){const q=(input?.value||'').trim().toLowerCase();const rows=data.filter(x=>[x.ship,x.name,x.next,x.note].filter(Boolean).join(' ').toLowerCase().includes(q)).sort((a,b)=>a.ship.localeCompare(b.ship));count.textContent=`${rows.length} ship assignment${rows.length===1?'':'s'} shown`;grid.innerHTML=rows.map(x=>`<article class="director-card"><div class="director-card-head"><span>${x.ship}</span><a href="ships/ship-guide.html?ship=${encodeURIComponent(x.shipId)}">Ship guide →</a></div><h2>${x.name}</h2><p class="director-dates">${fmt(x.from)}–${fmt(x.to)}</p>${x.next?`<p class="director-next"><strong>Next:</strong> ${x.next}</p>`:''}${x.note?`<p class="director-note">${x.note}</p>`:''}<p class="director-caution">Schedule may change for operational reasons.</p></article>`).join('')||'<p>No matching ship or director was found.</p>';}input?.addEventListener('input',render);const ship=new URLSearchParams(location.search).get('ship');if(ship&&input){const match=data.find(x=>x.shipId===ship);if(match)input.value=match.ship;}render();})();